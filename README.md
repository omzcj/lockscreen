# lockscreen

## Release

Update `VERSION` and push the change to `main`. The release workflow validates
the executable and publishes `lockscreen-VERSION.tar.gz` to a GitHub Release
tagged `vVERSION`.
